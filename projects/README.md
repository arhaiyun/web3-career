# 实践项目

每个实验或课程作业使用**独立子目录**，目录内包含：

- `README.md`：目标、前置依赖、安装与运行命令、所用网络（主网/测试网）
- 源代码与配置文件（如 `package.json`、`foundry.toml` 等）

命名建议：`01-hello-contract`、`dex-demo` 等，便于排序与检索。

不要在仓库根目录堆积多个项目的配置文件；保持「一目录一项目」。
